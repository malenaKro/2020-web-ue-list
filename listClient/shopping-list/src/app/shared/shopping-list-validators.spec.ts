import { ShoppingListValidators } from './shopping-list-validators';

describe('ShoppingListValidators', () => {
  it('should create an instance', () => {
    expect(new ShoppingListValidators()).toBeTruthy();
  });
});
